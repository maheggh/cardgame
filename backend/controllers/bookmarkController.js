const Ratings = require('../schemas/ratingSchema');
const mongoose = require('mongoose');

const bookmark = async (req, res) => {
    console.log('Creating bookmark'); // Log request body
};

const getBookmark = async (req, res) => {
    console.log('Creating bookmark'); // Log request body
};

const unBookmark = async (req, res) => {
    console.log('Creating bookmark'); // Log request body
};

const getAllUserBookmarks = async (req, res) => {
    console.log('Creating bookmark'); // Log request body
};


module.exports = { bookmark, getBookmark, unBookmark, getAllUserBookmarks };
